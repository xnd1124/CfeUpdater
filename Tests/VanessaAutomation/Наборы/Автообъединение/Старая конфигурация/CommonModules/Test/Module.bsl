Функция ТоварыТСД(ДанныеЗагрузки, АлкогольнаяПродукция)
        Результат = Новый Массив();
        ПозицияДанных.Вставить("Количество", ЧтениеXML.ЗначениеАтрибута("Quantity"));
        Возврат Результат;
КонецФункции

// ============================================================================
// ТЕСТ 1: Нет изменений
// Оба варианта идентичны предку → конфликта нет.
// ============================================================================
Процедура Тест_НетИзменений() Экспорт
        а = 1;
        б = 1;
        в = 1;
        
КонецПроцедуры

// ============================================================================
// ТЕСТ 2: Изменения только в одном варианте
// V1 не изменился, V2 изменил строку 2 → конфликта нет.
// ============================================================================
Процедура Тест_ИзмененияТолькоВОдномВарианте()
        а = 1;
        б = 1;
        в = 1;
КонецПроцедуры

// ============================================================================
// ТЕСТ 3: Изменения в разных частях текста
// V1 меняет строку 1, V2 меняет строку 3 → конфликта нет.
// ============================================================================
Процедура Тест_ИзмененияВРазныхМестах() Экспорт
        а = 1;
        б = 1;
        в = 1;
КонецПроцедуры

// ============================================================================
// ТЕСТ 4: Одинаковые изменения в одном месте
// Оба варианта заменили строку 2 на одно и то же → конфликта нет.
// ============================================================================
Процедура Тест_ОдинаковыеИзмененияОдноМесто() Экспорт
        а = 1;
        б = 1;
        в = 1;
КонецПроцедуры

// ============================================================================
// ТЕСТ 5: Оба варианта добавляют разные строки в начало (непересекающиеся)
// V1 меняет первую строку, V2 удаляет последнюю → конфликта нет.
// ============================================================================
Процедура Тест_НепересекающиесяДобавлениеИУдаление() Экспорт
        а = 1;
        б = 1;
        в = 1;
КонецПроцедуры

// ============================================================================
// ТЕСТ 6: Конфликт - одно место изменено по-разному
// V1 заменил строку 2 на «_измА», V2 - на «_измБ» → конфликт.
// ============================================================================
Процедура Тест_КонфликтОдноМестоРазныеИзменения() Экспорт
        а = 1;
        б = 1;
        в = 1;
КонецПроцедуры

// ============================================================================
// ТЕСТ 7: Конфликт - разные вставки в одно место
// Оба варианта вставляют разные строки перед Строка2 → конфликт.
// ============================================================================
Процедура Тест_КонфликтРазныеВставкиОднаПозиция() Экспорт
        а = 1;
        б = 1;
        в = 1;
КонецПроцедуры

// ============================================================================
// ТЕСТ 8: Конфликт - удаление строки в одном варианте и её изменение в другом
// V1 удалил строку 2, V2 изменил строку 2 → конфликт.
// ============================================================================
Процедура Тест_КонфликтУдалениеПротивИзменения() Экспорт
        а = 1;
        б = 1;
        в = 1;
КонецПроцедуры

// ============================================================================
// ТЕСТ 9: Конфликт - перекрывающиеся многострочные правки
// V1 меняет строки 1-2, V2 меняет строки 2-3 → конфликт (общая строка 2).
// ============================================================================
Процедура Тест_КонфликтПерекрывающиесяМногострочные() Экспорт
        а = 1;
        б = 1;
        в = 1;
        г = 1;
КонецПроцедуры

// ============================================================================
// ТЕСТ 10: Обе вставки в одно место, но содержимое идентично
// Оба варианта вставляют одно и то же → конфликта нет.
// ============================================================================
Процедура Тест_ОдинаковыеВставкиОднаПозиция() Экспорт
        а = 1;
        б = 1;
        в = 1;
КонецПроцедуры

// ТЕСТ 11: Вставка в одном, изменение совсем в другом месте → нет конфликта
// V1 вставляет между Строка2 и Строка3 (позиция 2),
// V2 меняет Строка2 → диапазон [1,2) не пересекается с вставкой в [2,2).
// (Стандартная семантика 3-way merge: независимые изменения сливаются.)
// ============================================================================
Процедура Тест_ВставкаПослеИзменённойСтроки() Экспорт
        а = 1;
        б = 1;
        в = 1;
КонецПроцедуры


Процедура Тест_КорректностиСборки() Экспорт
        Результат = Новый Массив();
        ПозицияДанных.Вставить("Количество", ЧтениеXML.ЗначениеАтрибута("Quantity"));
        Возврат Результат;
КонецПроцедуры
